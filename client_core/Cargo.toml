[package]
name = "client_core"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
client_consts = { path = "../client_consts" }
fltk = { version = "^1.1", features = ["fltk-bundled"] }
